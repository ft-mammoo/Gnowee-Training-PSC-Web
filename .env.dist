# --- SECRETS & CREDENTIALS ---

# 1. Django Secret Key (generate a new one securely!)
SECRET_KEY=REPLACE_ME_SECRET_KEY

# 2. Debug setting
DEBUG=True

# 3. Allowed Hosts (comma-separated list for production)
ALLOWED_HOSTS=127.0.0.1,localhost

# 4. PostgreSQL Database Credentials
#DB_NAME=gnowee_psc_project_db
#DB_USER=REPLACE_ME_DB_USER
#DB_PASSWORD=REPLACE_ME_DB_PASSWORD
#DB_HOST=localhost
#DB_PORT=5432

# Notes:
# - Copy this file to `.env` and fill in the values (do NOT commit `.env`).
# - For Django: generate a secure secret key. Example (Python):
#   python -c "import secrets; print(secrets.token_urlsafe())"
# - Set DEBUG=False in production and configure ALLOWED_HOSTS accordingly.
